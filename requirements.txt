# LLM + Agent framework
anthropic>=0.49.0
langgraph>=0.3.0
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-mcp-adapters>=0.1.0
langsmith>=0.3.0

# Web search (optional — used by drafting node when TAVILY_API_KEY is set)
langchain-tavily>=0.1.0

# MCP servers (run as subprocesses via mcp-adapters)
# github-mcp-server and notion-mcp-server are Node.js binaries installed separately

# API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
httpx>=0.28.0
python-multipart>=0.0.20
sse-starlette>=2.2.0
slowapi>=0.1.9

# Auth + encryption
cryptography>=44.0.0
python-jose[cryptography]>=3.3.0
itsdangerous>=2.2.0

# Database
supabase>=2.15.0

# Config
pydantic[email]>=2.11.0
pydantic-settings>=2.8.0
python-dotenv>=1.1.0

# Frontend
streamlit>=1.44.0

# Dev tools
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.1.0
pytest-httpx>=0.36.0
ruff>=0.11.0
black>=25.1.0
isort>=5.13.0
mypy>=1.15.0
types-python-jose>=3.3.4
